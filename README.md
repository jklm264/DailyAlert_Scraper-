To run do:

```bash
# Build Virtual env
virtualenv --python=python2 venv
source venv/bin/activate
# Install needed packages
pip install -U pip pip
pip install -r requirements.txt
# Run script
python DailyAlert.py
```
